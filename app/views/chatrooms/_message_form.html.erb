<%= simple_form_for [ user, chatroom, message ], remote: true do |f| %>
  <%= f.input :content, label: false, placeholder: "Message # #{chatroom.users.find { |u| u != @user }.first_name.capitalize}" %>
<% end %>
